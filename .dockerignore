# Local test data
data/

# VCS
.git/
.gitignore

# Helm / k8s — not needed for the jar build
helm/

# Maven build output
**/target/**

# Docker needs the already-built service runner jar
!dwc-dp-analyser-service-app/target/
!dwc-dp-analyser-service-app/target/*-runner.jar

# Scripts for local compose, not part of the build
scripts/

# IDE / editor
.idea/
*.iml
.vscode/

# OS cruft
.DS_Store

# Docker/compose files themselves
docker-compose.yml
Dockerfile
.dockerignore

# Taskfile
Taskfile.dist.yml
