apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dwc-dp-analyser.fullname" . }}-logback
  labels:
    {{- include "dwc-dp-analyser.labels" . | nindent 4 }}
data:
  logback.xml: |
{{ .Values.log.logback.config | indent 4 }}
